services:
  redis:
    image: redis:7-alpine
    command: redis-server --save "" --appendonly no --loglevel warning
    ports:
      - "127.0.0.1::6379"
    tmpfs:
      - /data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 1s
      timeout: 2s
      retries: 20
